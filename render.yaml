version: 1

# ==================== PostgreSQL Database ====================
services:
  - type: pgsql
    name: inmueblebot-db
    plan: free
    databaseName: inmueblebot
    region: frankfurt   # o la región más cercana a ti

# ==================== Redis ====================
  - type: redis
    name: inmueblebot-redis
    plan: free
    region: frankfurt

# ==================== FastAPI Backend ====================
  - type: web
    name: inmueblebot-api
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    region: frankfurt
    healthCheckPath: /health
    autoDeploy: true

    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: inmueblebot-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: inmueblebot-redis
          property: connectionString
      - key: ENVIRONMENT
        value: production
      - key: WHATSAPP_WEBHOOK_VERIFY_TOKEN
        sync: false

# ==================== Celery Worker ====================
  - type: worker
    name: inmueblebot-celery-worker
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    region: frankfurt
    startCommand: celery -A app.celery_app worker --loglevel=info

# ==================== Celery Beat (Scheduler) ====================
  - type: worker
    name: inmueblebot-celery-beat
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    region: frankfurt
    startCommand: celery -A app.celery_app beat --loglevel=info